services:
  - type: web
    name: aashram-project
    env: node
    buildCommand: npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
